import React, { Component } from 'react';
import {
  Router,
  Route,
  browserHistory
} from react-router;


const App = (props) => {
  <Router history={browserHistory}>
    //Routes go here
  </Router>
}
export default App;
